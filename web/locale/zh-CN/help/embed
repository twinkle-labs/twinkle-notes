* 嵌入其他内容

当你在一条笔记上添加图片的时候，图片将会独立存储，在笔记里会出现图片引用的代码，如下：
```
[image:/blob/685142f10da29bcf912413e3c832709ad7902b06ae265e835e324680fd3569fc]
```
其他的嵌入内容，如PDF文件，视频文件，音频文件，框图，画图等等，都是同样的引用方式。

** 特殊例子

在笔记文本中写 `[hidden:你想隐藏的文字]`, 将会生成
[hidden:你想隐藏的文字]。
对于那些默认状态下不应该显示的信息，比如答案、密码等，可以用这种方式隐藏。

